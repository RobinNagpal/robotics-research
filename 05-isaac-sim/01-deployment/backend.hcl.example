# Copy to backend.hcl and set a globally-unique bucket name (the one the
# bootstrap step created). Then: terraform init -backend-config=backend.hcl

bucket       = "isaac-sim-tfstate-CHANGEME"
key          = "isaac-sim/01-deployment/terraform.tfstate"
region       = "us-east-1"
encrypt      = true
use_lockfile = true
